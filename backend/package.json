{
	"name": "backend",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"build": "npm run transpile",
		"server": "node ./dist-server/index.js",
		"start-dev": "npm-run-all build server",
		"dev": "nodemon",
		"clean": "rimraf dist-server",
		"transpile": "babel server --out-dir dist-server",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@babel/cli": "^7.18.9",
		"@babel/core": "^7.18.9",
		"@babel/preset-env": "^7.18.9",
		"express": "^4.18.1",
		"nodemon": "^2.0.19",
		"npm-run-all": "^4.1.5",
		"rimraf": "^3.0.2",
		"socket.io": "^4.5.1"
	},
	"babel": {
		"presets": [
			"@babel/preset-env"
		]
	},
	"nodemonConfig": {
		"exec": "npm run start-dev",
		"watch": [
			"server/*"
		]
	}
}
